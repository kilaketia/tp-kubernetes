apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kpage-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: 
    http:
      paths:
      - path: /
        backend:
          service:
            name: kpage-service
            port:
              number: 80
